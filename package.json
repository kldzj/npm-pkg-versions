{
	"name": "npm-pkg-versions",
	"version": "1.1.0",
	"description": "Asynchronously list npm package versions",
	"keywords": [
		"npm",
		"package",
		"version",
		"versions",
		"list",
		"async"
	],
	"main": "dist/index.js",
	"files": [
		"dist"
	],
	"repository": "https://github.com/kldzj/npm-pkg-versions",
	"bugs": "https://github.com/kldzj/npm-pkg-versions/issues",
	"license": "MIT",
	"scripts": {
		"test": "jest",
		"build": "tsc",
		"prepublishOnly": "npm run test && npm run build",
		"postpublish": "git push origin master --follow-tags"
	},
	"dependencies": {
		"cross-fetch": "^3.0.6"
	},
	"devDependencies": {
		"@types/jest": "^26.0.15",
		"jest": "^26.6.1",
		"ts-jest": "^26.4.3",
		"typescript": "^4.0.5"
	}
}
